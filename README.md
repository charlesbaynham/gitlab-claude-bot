# gitlab-claude-bot

A polling daemon for gitlab.com: it picks up the bot account's to-dos and new comments on its merge requests, runs Claude Code in a throwaway Docker container per job, and pushes the result as the bot.

Work in progress.
